# Python
__pycache__/
*.pyc

# npm — only used to regenerate oui.json; the JSON itself IS committed
node_modules/

# Capture data — NEVER commit (privacy / OPSEC: a WiGLE CSV pinpoints where you were)
*.csv
*.csv.gz

# Generated analysis outputs (regenerate from a capture; don't track)
*.report.html
report.html
survey.geojson
survey.kml
survey.html
# NOTE: only the generated wigle_map.* OUTPUTS — never the wigle_map.py source.
wigle_map.geojson
wigle_map.kml
wigle_map.html
WigleWifi_*.html
WigleWifi_*.geojson
WigleWifi_*.kml
*.geojson
*.kml
*.db
out.json
*_out.json

# Home-base / exclusion outputs (contain YOUR home device MACs + locations — never commit)
exclude.txt
*_exclude.txt
*_devices.csv
*_bases.json

# Styled reports (wigle_report2 / brief) and pentest target lists
recon.html
threat.html
report.html
report_*.html
brief_*.html
targets.csv
